# Frontend environment variables
# Copy this to .env.local for local development (Vite reads .env.local automatically)

# The deployed backend API URL (Render)
# Only needed in production builds — in dev, the Vite proxy handles /api
VITE_API_URL=https://your-backend.onrender.com
